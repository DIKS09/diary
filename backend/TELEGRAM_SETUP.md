# Настройка Telegram бота для обратной связи

## Инструкция по настройке

### Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите название вашего бота (например: "My Diary Feedback Bot")
4. Введите username бота (должен заканчиваться на 'bot', например: "my_diary_feedback_bot")
5. **Скопируйте токен**, который выдаст BotFather (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получение Chat ID

1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему команду `/start`
3. **Скопируйте ваш ID** (это будет число, например: `123456789`)

### Шаг 3: Настройка .env файла

1. Создайте файл `.env` в папке `backend` (если его нет)
2. Добавьте в него следующие строки:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

Пример заполненного файла:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

### Шаг 4: Запуск бота

1. Найдите вашего бота по username в Telegram
2. Нажмите кнопку "Start" или отправьте команду `/start`
3. Теперь бот может отправлять вам сообщения!

### Шаг 5: Перезапуск backend

После настройки .env файла перезапустите backend сервер:

```bash
cd backend
# Остановите текущий процесс (Ctrl+C)
# Запустите снова
python app.py
```

## Проверка работы

1. Откройте сайт дневника
2. Нажмите на иконку обратной связи (💬) в правом верхнем углу
3. Заполните форму и отправьте тестовое сообщение
4. Проверьте, что сообщение пришло в Telegram

## Устранение проблем

### Ошибка "Telegram бот не настроен"
- Проверьте, что файл `.env` находится в папке `backend`
- Убедитесь, что в файле нет лишних пробелов
- Проверьте, что токен и chat_id правильно скопированы

### Сообщения не приходят
- Убедитесь, что вы нажали "Start" у вашего бота
- Проверьте правильность токена бота
- Проверьте правильность chat_id
- Убедитесь, что есть подключение к интернету

### Как получить chat_id другим способом

Если @userinfobot не работает, можно получить chat_id так:

1. Напишите вашему боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите в ответе поле `"chat":{"id":123456789}` - это ваш chat_id

## Дополнительно

- Вы можете настроить бота на отправку в группу или канал вместо личных сообщений
- Для этого добавьте бота в группу/канал и используйте chat_id группы/канала
- Chat_id группы начинается с минуса (например: `-123456789`)

