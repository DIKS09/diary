ЯРЧЕ# 📖 Мой Личный Дневник

Современное full-stack веб-приложение для ведения личного дневника с красивым интерфейсом.

## ✨ Возможности

- 📝 Создание записей с заголовками и содержанием
- ✏️ Редактирование существующих записей
- 💾 Хранение в MongoDB базе данных
- 🎨 Красивый современный интерфейс в бирюзовых тонах
- 📱 Адаптивный дизайн для всех устройств
- 🎭 Случайные эмодзи для каждой записи
- 🗑️ Удаление записей
- 📖 Развернуть/свернуть длинные записи
- 🔄 Real-time обновление данных
- ⚡ REST API на Python/Flask

## 🏗️ Технологический стек

### Frontend
- React 18
- React Hooks (useState, useEffect)
- CSS3 с анимациями и градиентами
- Fetch API для работы с backend

### Backend
- Python 3.x
- Flask (REST API)
- MongoDB (база данных)
- Flask-CORS (для cross-origin запросов)
- PyMongo (MongoDB драйвер)

## 📋 Предварительные требования

1. **Node.js** (v14 или выше)
2. **Python** (v3.8 или выше)
3. **MongoDB** (v4.0 или выше) - должен быть установлен и запущен локально

## ⚙️ Настройка переменных окружения

### Обязательная настройка для функций обратной связи и новостей

1. **Скопируйте пример файла конфигурации:**
   ```bash
   copy backend\env.example backend\.env
   ```

2. **Получите API ключи:**

   #### Telegram Bot (для обратной связи):
   - Откройте Telegram и найдите `@BotFather`
   - Отправьте `/newbot` и следуйте инструкциям
   - Скопируйте полученный **токен бота**
   - Найдите `@userinfobot` и отправьте `/start`
   - Скопируйте ваш **chat_id**

   #### News API (для новостей):
   - Зарегистрируйтесь на https://newsapi.org/register
   - Получите бесплатный **API ключ** (500 запросов/день)

3. **Откройте `backend/.env` и вставьте ваши ключи:**
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
   TELEGRAM_CHAT_ID=ваш_chat_id
   NEWS_API_KEY=ваш_ключ_newsapi
   ```

4. **Перезапустите серверы** после настройки .env

> **Примечание:** Файл `.env` не отправляется в Git для безопасности. Каждый разработчик должен создать свой собственный файл `.env` с личными ключами.

## 🚀 Быстрый запуск

### Автоматический запуск (Windows)

Просто запустите файл `start.bat` - он автоматически:
- Проверит и запустит MongoDB
- Установит зависимости backend
- Запустит Flask сервер
- Установит зависимости frontend
- Запустит React приложение

```bash
start.bat
```

### Ручной запуск

#### 1. Запустите MongoDB

```bash
# Windows (если установлен как служба)
net start MongoDB

# Или запустите вручную
mongod
```

#### 2. Настройте Backend

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
python app.py
```

Backend запустится на `http://localhost:5000`

#### 3. Настройте Frontend

Откройте новый терминал:

```bash
# Установите зависимости
npm install

# Запустите приложение
npm start
```

Frontend откроется в браузере на `http://localhost:3000`

## 📁 Структура проекта

```
diary-site/
├── backend/                    # Backend на Python
│   ├── app.py                 # Flask приложение
│   ├── requirements.txt       # Python зависимости
│   ├── .env.example          # Пример конфигурации
│   └── README.md             # Backend документация
├── public/                    # Публичные файлы
│   └── index.html
├── src/                       # Frontend исходники
│   ├── components/
│   │   ├── DiaryEntry.js     # Компонент записи
│   │   ├── DiaryEntry.css
│   │   ├── EntryForm.js      # Форма создания
│   │   └── EntryForm.css
│   ├── App.js                # Главный компонент
│   ├── App.css
│   ├── index.js
│   └── index.css
├── package.json
├── start.bat                  # Скрипт автозапуска
└── README.md
```

## 🔌 API Endpoints

### GET `/api/entries`
Получить все записи из базы данных

### POST `/api/entries`
Создать новую запись
```json
{
  "title": "Заголовок",
  "content": "Содержание записи"
}
```

### PUT `/api/entries/:id`
Обновить существующую запись
```json
{
  "title": "Новый заголовок",
  "content": "Новое содержание"
}
```

### DELETE `/api/entries/:id`
Удалить запись по ID

### GET `/api/health`
Проверка работы сервера

## 💡 Использование

1. Убедитесь, что MongoDB запущена
2. Запустите backend и frontend серверы
3. Откройте `http://localhost:3000` в браузере
4. Нажмите "Новая запись" для создания записи
5. Введите заголовок и содержание
6. Нажмите "Сохранить запись"
7. Все записи сохраняются в MongoDB
8. Для удаления записи нажмите на иконку корзины

## 🎨 Особенности дизайна

- Современный glass morphism эффект
- Плавные анимации и переходы
- Градиенты в бирюзово-зеленых тонах
- Адаптивный дизайн для мобильных устройств
- Интерактивные элементы с hover-эффектами
- Красивые тени и свечения

## 🐛 Решение проблем

### MongoDB не запускается
- Убедитесь, что MongoDB установлен
- Проверьте, что порт 27017 свободен
- Попробуйте запустить `mongod` вручную

### Backend не подключается к MongoDB
- Проверьте, что MongoDB запущен
- Убедитесь, что используется правильный порт (27017)
- Проверьте логи в консоли backend

### Frontend не может подключиться к Backend
- Убедитесь, что backend запущен на порту 5000
- Проверьте настройки CORS
- Откройте консоль браузера для деталей ошибки

## 📝 Сборка для продакшена

```bash
npm run build
```

Оптимизированные файлы будут в папке `build/`

Приятного использования! ✨

